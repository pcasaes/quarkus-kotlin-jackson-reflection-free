# Reflection-free serializers are enabled by default in Quarkus 3.37
# This can be toggled using Maven profiles (see pom.xml)
# Use: mvn test -P reflection-free-enabled   (default)
# Use: mvn test -P reflection-free-disabled
